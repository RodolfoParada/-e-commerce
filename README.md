  #### 1. crea la carpeta Theory para guardar las tasks.
  #### 2. crear la carpeta Practice para guardar el ejercicio antes de desarrollarlo
   #### 3. se desarrolla el ejercicio y se resuelve a nivel intregracion con test funcionales.
    #### dependencias adicionales y actualizadas
   ```
   npm install --save-dev jest
   ```
   ```
   npm install --save-dev supertest sequelize-test-helpers
   ```
   ```
   npm install express
   ```
   ``` 
   npm install jsonwebtoken
   ```

   #### Para levantar los test
     ```
     npm test
     ```